# M2.11 Correction Brief - Verifier v2 Stage Replay

Date: 2026-08-27
Status: Complete

## Observed Failure

Local schema and semantic tests cover the Review Verifier v2 contract, but the current provider and selected model have not yet demonstrated that they can return the exact bounded v2 artifact in one isolated call.

## User Artifact

One recorded provider result proving or disproving that the selected model can produce a structurally valid, semantically independent Review verification artifact without running or mutating a Meeting.

## Baseline

The earlier Stage Replay 003 passed the v1 contract. The v2 contract adds dual lineage and semantic-independence checks, so that result cannot close the current provider gate.

## Smallest Hypothesis

The selected connected model can satisfy the fixed Review Verifier fixture v2 and 600 output-token cap in exactly one provider call.

## Expected Information Gain

The result will distinguish provider/model compatibility from local orchestration defects and expose any exact schema or semantic mismatch without paying for a full meeting.

## Acceptance Checks

- Exactly one provider call and no automatic retry.
- The response identifies fixture version 2.
- The v2 schema and semantic checks pass, or the first diagnostic is captured verbatim as evidence.
- Input tokens, output tokens, estimated cost, and latency are recorded.
- No Meeting record is created or modified.

## Cost Boundary

User-authorized maximum estimated provider cost: USD 0.01. The replay has a 600 output-token cap and must stop after the first result.

## Stop Condition

Stop after one success or one failure. Do not retry an unchanged prompt, change models, or run a full Review/Plan flow in this gate.

## Result

The hypothesis passed. Anthropic `claude-haiku-4-5-20251001` returned a valid fixture-v2 artifact in one call: 597 input tokens, 224 output tokens, 3.1 seconds, and a `$0.0034` application estimate. Both `lineage` and `semantics` independently supported `change-replay-1`; no unresolved item remained. Meeting History stayed at 10 records. No retry or additional provider call followed. See [Stage Replay 008](../evaluations/2026-08-27-v0.11-verifier-v2-stage-replay-008.md).

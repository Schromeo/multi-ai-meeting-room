# M2.12 Correction Brief - Receipt Access and Estimate Provenance

Date: 2026-08-27
Status: Complete locally

## Observed Failure and Artifact

S1 009 produced a usable resume, but its download could not be retrieved through the in-app browser. Only visible output and reconstructed prompt metadata were archived. The $0.0038 app estimate also differed from the $0.00547 model-price calculation because the runtime uses provider-wide rates.

## Smallest Hypothesis and Acceptance

- Expose the exact existing server receipt as selectable, read-only text with a copy action and manual fallback. Keep download and page-memory semantics; no new storage, API endpoint, rerun, or historical receipt reconstruction.
- Include effective input/output rates and per-rate default/override provenance in new Replay responses, including failed S1 receipts. Mark estimates as provider-wide, not model-verified or invoices. Preserve existing arithmetic and unknown usage.
- Test complete receipt rendering, copy success/rejection, old receipt behavior, and rate provenance including overrides and failures using local mocks. No new price catalog or model-selection logic.
- Do not change the fixed task/prompt/cap, meeting phases, credentials, or oracle boundary. No major frontend redesign; existing source remains the recovery point.

## Cost, Information Gain, and Stop

Zero live provider calls. Do not rerun S1 009 or consume prior budget. Exit when evidence can be copied without download and estimates disclose their basis. This improves audit access, not model quality; M3/R6 and unique multi-model value remain untested. Update bilingual docs and stop before any new paid comparison.

## Result

Added a collapsed, read-only full receipt and copy action with selection fallback. Existing downloads and unknown/failed usage are preserved. New Replay receipts record effective provider rates and per-rate default/override sources; old receipts say provenance was not recorded. Prices, prompts, call counts, and persistence are unchanged. Build and all 40 tests pass; clipboard failure is tested locally, not in the live browser. The existing localhost:3001 service responds successfully. Zero live provider calls. Baseline 009 files remain unchanged; no missing historical metadata was reconstructed as fact. Return to the product comparison decision, not another infrastructure slice.

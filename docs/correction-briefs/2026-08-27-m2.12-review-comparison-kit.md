# M2.12 Correction Brief - Offline Review Comparison Kit

Date: 2026-08-27
Status: Complete (offline preparation only)

## Observed Failure

Benchmark 005 completed mechanically while missing an explicit metric constraint and rewarding a cosmetic rewrite. Stage Replay 008 proves a positive fixture can satisfy the Verifier v2 format, not that cross-review improves an artifact. No matched strong-single-model or manual-copy baseline exists.

## User Artifact

A reusable comparison kit: identical public inputs for three Review cases, separate evaluator-only issue anchors and example corrected artifacts, fair baseline prompts, and a rubric that separates discovery, repair, adoption, and cross-review attribution.

## Baseline

Existing reports are diagnostic evidence, not matched comparisons. Baseline result slots remain explicitly unrun; author-written golden examples are not model results.

## Smallest Hypothesis

Freezing inputs and acceptance criteria before another call makes the next comparison interpretable without changing the product runtime.

## Expected Information Gain

Zero provider calls. Local tests establish that source anchors exist, example changes are applicable through the production Change Set parser, and model-facing exports exclude evaluator answers.

## Acceptance Checks

- Three synthetic, versioned cases: resume, requirements document, technical plan.
- Public inputs and private-to-the-evaluation answer keys are separate files; no actual personal data or credentials.
- The same source bundle and deliverable instruction serve single-model, manual-copy, and app runs.
- Material issue detection, artifact repair, false positives, human edits, and causal cross-review changes are scored separately.
- Missing observations stay unknown; no automatic semantic truth judge or fabricated baseline result.
- An offline command prints inputs/prompts only, makes no requests, and writes no Meeting data.
- Focused tests and documentation checks pass; both documentation languages are updated.

## Cost Boundary

Zero provider/API calls. No deployment, UI redesign, new dependencies, model selection, or full-room evaluation.

## Stop Condition

Stop once the kit passes local validation. Another provider run requires a named baseline arm, exact model configuration, information-gain question, and fresh budget authorization. Do not build a generic evaluation service.

## Result

The preparation hypothesis passes: three versioned public cases and separate evaluator examples are saved, the local exporter excludes answer keys, and seven targeted tests pass through the production artifact contract. The full build and thirty-six tests pass, as does ESLint. No provider call or runtime change occurred. The bilingual comparison rubric leaves every actual baseline NOT RUN; model quality and differentiated value remain unknown. The next permitted path is one newly authorized S1 baseline, not an automatic multi-model run.

# M2.12 Correction Brief - Fixed S1 Entry

Date: 2026-08-27
Status: Complete locally; real S1 NOT RUN

## Observed Failure and Baseline

The offline comparison kit exports the correct S1 prompt, but the app's only one-call entry runs a Verifier fixture. The normal Review requires multiple calls. Neither can capture a plain-text single-model baseline using session BYOK without changing the experiment or extracting a key.

## User Artifact and Hypothesis

Extend the existing development Replay selector with only `resume-truth-v1` S1. One explicit action reuses the normal provider adapter, captures plain text and a credential-free evidence receipt, and never writes a Meeting. Reuse the exact offline prompt builder; no oracle enters the server path. This is a fixed evaluation entry, not a generic prompt playground.

## Acceptance and Information Gain

- Existing Verifier behavior remains unchanged.
- S1 uses one server-owned public fixture, one call, a 2,400-output-token cap, and no retry.
- Return the exact system/user prompt, input/prompt hashes, model/settings, raw output, and usage when known; a captured response is unscored, not a semantic pass.
- Preserve partial failed output with unknown usage; download only the credential-free receipt, not keys or Meeting state.
- Reject injected prompt/case/output-limit fields before provider work.
- Mocked tests prove the boundary, and local UI inspection makes no call.

## Cost and Stop

Zero real provider calls during implementation. Current browser has zero Connections. A real S1 run needs a new connection, explicit model choice, and fresh budget approval. Stop after this narrow entry and its tests; no model marketplace, batch runner, new baseline protocol, deployment, or broader UI work.

## Result

The fixed entry, shared prompt, downloadable receipt, and partial-failure path pass local validation. Build, all 38 tests, and ESLint pass; browser inspection confirms that the S1 action is disabled with zero Connections. No provider call was made. Real S1 quality and comparison results remain unrun pending connection/model/budget approval.

# Human Plan Revision

Status: Local implementation complete; browser/provider acceptance remains open

Result: build, 48 offline tests and lint pass. Original provenance, multi-day edits, exact approval/history and injected save-handler failure are covered. This is not a real IndexedDB fault or user-adoption test. Zero API calls. Teaching quality, reading effort and comparative advantage remain unknown. Stop implementation here; retain real-browser and provider-quality gates.

Observed gap: the completed detailed Plan exposes day-linked concerns but only whole-plan approve/reject. A useful reviewer suggestion cannot become an adopted correction inside the room. Baseline: the first M2.13 slice preserves AI days and review, but requires a new room for any changed content.

Smallest hypothesis: the Chair can edit one day locally before approval, validate the complete revised plan against the unchanged day/MEU/time contract, inspect the original, and freeze the exact revised artifact. Keep the original plan and its model review immutable; never imply the model reviewed human edits.

Boundary: no paid calls, automatic rewriting, new rounds, changed contract, external question catalog, or generic editor framework. Reuse local history and approval. Edits include tasks, timing, completion and adjustment text. All other days remain unchanged. Saving must succeed before publishing the revision; unsaved edits block approval. Approved/rejected plans are read-only. No new claim of teaching quality or comparative advantage.

Acceptance: invalid/duplicate/under-workload/over-time days and stale provenance reject; original remains intact; successive day edits preserve other days; restoration and approval match the displayed revision; failed save keeps the draft and previous saved version. Offline fixture/rendering and route regressions only; real browser and provider quality gates remain open. Stop after this adoption gap, not a new orchestration milestone.

Backup: `/private/tmp/meeting-room-before-plan-human-edit-20260827.tar.gz`.

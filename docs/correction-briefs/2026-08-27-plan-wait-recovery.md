# Plan Wait and Explicit Recovery

- Failure: Live010's Builder produced six valid days before the app's180s timer aborted it. A prior format recovery then left one reserved call slot, blocking the two-call artifact recovery. Raising the monetary authorization alone does not fix either boundary.
- Artifact: the same complete12-day plan, with an actual-plan independent review, preserving the six saved days and original failures. Human approval remains pending.
- Hypothesis: removing Plan artifact wall-clock aborts and explicitly renewing the one unused artifact recovery allowance lets valid long-running generation finish without a full-room rerun.
- Scope: no application deadline for Plan Builder/reviewer/amendment/recheck; ordinary discussion retains90s. Cumulative Plan time becomes telemetry, not a stop. Preserve per-call output caps, token limits, manual cancellation, source matching and one explicit recovery. No prompt/model changes, autonomous retry, task splitting or ledger platform.
- Recovery: human action saves a new budget snapshot before any call, changes only time enforcement and the minimum call-slot ceiling needed for one existing interrupted artifact transition. Preserve all prior reservations conservatively; do not claim they equal actual billed calls. Existing input/output limits remain. A second artifact transition cannot renew again. Completed/reviewed/stale/mismatched plans cannot enter this path.
- Acceptance: simulated requests beyond180s complete; manual cancellation still interrupts; generic90s policy unchanged; legacy numeric budgets round-trip; zero time limit explicitly means no cumulative cutoff; stopped Live010-like records can continue once, retain old days, and use at most2calls. Save failure means no call. Display wait and recovery meaning honestly.
- Cost: local tests make no provider calls. User permits a new experiment if needed, under the existing cumulative$5 authorization, not another$5. Prefer two-call missing-days/review continuation; no unrelated paid tests. Historical failed usage remains unknown; reserve conservatively before another call and stop if allowance cannot be bounded.
- Stop: one informative continuation or a real blocker; archive the result, do not automatically rerun to force success. Removing our timer does not remove provider, transport or host limits and does not guarantee completion or zero cost after cancellation.

## Live Continuation Preflight

- Both connections reverified by the user; restore the same History11 record and match GPT-5 snapshot/Opus4.7 Seats through the UI. No key is read or archived. Model selection resets the active view, so reopen the saved record after matching; do not press Start meeting.
- Corrected an offline-fixture gap found before any paid call: real `stopProtocol` stores phase stopped with status complete. Permit this explicit interrupted-artifact recovery, but not a completed deliverable. Test uses the production stop function.
- Maximum two requests: missing Days7-12 Builder <=9,600 output tokens, actual-plan review <=6,000. Using010's checked standard rates, their output component is <=$0.246, including GPT reasoning in its output cap. Inputs are additional; retain a conservative reserve for all six earlier requests, including failed ones. No tools, extra rounds, amendment or new seats in this continuation. The original cumulative$5 ceiling remains; invoices and failed-call usage are not available, so displayed cost is not an authoritative remaining balance.

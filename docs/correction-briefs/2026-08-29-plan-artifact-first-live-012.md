# Artifact-first Plan Live 012 Correction Brief

- **Observed drift:** Detailed Plan advertises one Builder and one independent Reviewer, but a fresh room still enters the generic proposal and cross-review protocol before synthesis. The launch preview therefore permits six provider calls for two Seats, spending budget before the requested artifact exists.
- **User artifact:** a complete Chinese 12-day LeetCode plan with at least 10 Medium-equivalent units per day, concrete problem metadata, time allocations, completion rules, spaced review and overload adjustments; followed by an independent review of that actual plan.
- **Correction:** Detailed Plan starts directly in `synthesis`. `gpt-5.6-sol` is the Builder and `claude-fable-5` is the independent Reviewer. Generic proposals, cross-reviews and Observer work are not part of this Task Pack launch.
- **Fixed case:** reuse the archived 12-day objective, 10 Medium-equivalent units per day and the explicit 360-minute test assumption. Do not silently soften workload or claim external problem-metadata verification.
- **Call and retry boundary:** at most two provider calls total: Builder once, then Reviewer once only after a validated complete artifact exists. No automatic retry, amendment, recheck, third Seat or Observer call. A failed Builder ends the run before Reviewer. Recovery requires a new explicit authorization.
- **Cost boundary:** user-authorized ceiling is USD 1.00. The application cannot enforce provider invoices, so the enforceable controls are two calls, 16,000 Builder output tokens, 6,000 Reviewer output tokens and bounded inputs. At current published list rates, their output ceilings alone are approximately USD 0.32 and USD 0.30 respectively; input, caching, taxes and provider accounting remain provider-authoritative.
- **Acceptance:** preflight reports two calls; the saved protocol begins at synthesis with exactly two pending system-role Seats; regression tests prove ordinary Decide/Review protocols are unchanged; live history preserves model, stage, finish, usage and parser diagnostics. Completion requires 12 accepted days and a valid Fable review. Semantic usefulness is audited separately from mechanical completion.
- **Interpretation:** this is a quality-forward product candidate, not a causal PLAN-03 single-variable experiment, because both model assignment and protocol entry change. Archive success or failure without retrying the unchanged request.

## Interim Closeout

The direct path exposed two pre-provider contract defects: the API still required generic discussion history, then the Plan parser rejected the valid version-zero source State. Both are fixed locally; build, 61 tests and lint pass. History 12 made no provider call. History 13 aborted on the first empty checkpoint; a Sol call may have started before cancellation, so its usage is unknown. Fable did not run and product quality remains untested. See [Live 012](../evaluations/2026-08-29-plan-artifact-first-live-012.md). A further two-call run needs renewed authorization.

Renewed authorization produced [Live 014](../evaluations/2026-08-29-plan-artifact-first-live-014.md). Sol completed all 12 days with zero rejected rows; Fable completed its provider response but the review was rejected as invalid JSON. Exactly two calls ran and no retry followed. D-059 is mechanically live-verified; independent-review and multi-model value Gates remain failed.

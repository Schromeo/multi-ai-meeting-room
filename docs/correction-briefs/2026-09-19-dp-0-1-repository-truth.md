# DP-0.1 Product and Repository Truth Correction Brief

- **Observed failure:** the committed repository still identifies its package as `site-creator-vinext-starter` at version `0.1.0`, tracks both npm and pnpm lockfiles, and gives npm-only setup commands even though the workspace is pnpm-based. The root README still describes v0.10c and Review as the next slice, while Roadmap and Handoff describe already committed work as an uncommitted working tree. No repository license state is declared. Current validation claims are scattered and do not give one reproducible command baseline.
- **User artifact:** a repository whose name, development-version policy, package-manager policy, license state, current milestone, credential boundary, and exact local validation status can be understood from the root README and canonical project documents without reconstructing project history.
- **Baseline:** HEAD is `a9a8638` on `main`, with no tags and a clean working tree before this brief. `package.json` is private but uses the starter name and `0.1.0`; `package-lock.json` and `pnpm-lock.yaml` are both tracked; `pnpm-workspace.yaml` is present; no `LICENSE*` file is tracked. README claims v0.10c and npm commands. DP-0.1 is the only Current forward milestone. The last recorded validation says build, 63 tests, and lint pass while three Cloudflare ambient type errors remain, but that result must be rerun in the current checkout.
- **Smallest hypothesis:** changing only repository metadata and current-facing documentation, selecting pnpm as the sole package manager, declaring the code private and unlicensed, and recording one fresh command matrix will eliminate false project-state claims without changing runtime behavior or historical evidence.
- **Expected information gain:** fresh install, build, test, lint, and type-check results will distinguish current reproducible facts from inherited claims. The inventory will also show whether DP-0.2 must begin with package-script portability or type-environment work. No model/provider call is needed.
- **Change boundary:** rename the package to `multi-ai-meeting-room`; use a non-release development version until the owner creates a tagged release; add an exact `packageManager` declaration; make pnpm the only current package-manager path; remove the npm lockfile; declare `UNLICENSED`/all rights reserved without inventing an open-source license; update current README, metadata, Roadmap, Handoff, milestone status, Decisions, Devlog, and Chinese mirrors. Preserve historical evaluation reports and their original commands or version labels.
- **Acceptance checks:** no live/current file uses the starter identity or calls committed work uncommitted; root setup uses pnpm only; one canonical lockfile remains; the absence of an open-source grant is explicit; current source is identified by milestone and commit rather than a fabricated release; credential guidance says session BYOK is ephemeral and public deployments must not expose workspace-funded keys before DP-0.6; fresh `pnpm install --frozen-lockfile`, build, tests, lint, type check, and `git diff --check` are recorded with exact outcomes.
- **Cost boundary:** zero provider calls, zero API spend, no browser record mutation, no deployment, no dependency upgrade, and no external account or service change. Runtime behavior, Review/Plan artifacts, and legacy evaluation evidence stay unchanged.
- **Stop condition:** stop after the smallest truth patch and command baseline. Do not repair Cloudflare types, cross-platform shell syntax, first-run UX, public API enforcement, or historical artifacts in this slice. If a license grant or release number requires owner intent, record the restrictive/no-release state instead of guessing.

# DP-0.3 First-run Entry Correction Brief

Date: 2026-09-24
Status: Active; first local entry/Solo slice implemented, milestone acceptance incomplete
Pre-UI backup: local branch `backup/dp-0-3-pre-ui-2026-09-24` at `97b865a`

- **Observed failure:** the source-level fresh-session fixture opens in Review with a Review-specific objective, the header presents Setup → Agenda → Meeting → Decision, and the agenda exposes only Review and Decide / Plan. `canStart` requires at least two ready Seats for every mode. Switching those two modes changes only `taskMode`, leaving the previous mode's objective in place. See the [first-run baseline](../evaluations/2026-09-24-dp-0-3-first-run-baseline.md).
- **User artifact:** an understandable first screen with Chat, Ask the Room, Drop an Artifact, and Browse Packs entries; one usable Connection can start a Solo conversation; each mode receives its own objective draft and no other mode's task text. The existing Review and Plan records remain readable.
- **Baseline:** the committed `97b865a` implementation has a working bounded multi-Seat Review/Decide room and session Connections but no Solo protocol or four-way entry. The current screen and state paths are a code fixture, not a completed fresh-user walkthrough or browser accessibility claim.
- **Smallest hypothesis:** moving the choice of user intent before the room composer, isolating mode drafts, and adding a bounded one-Connection Solo path will let a first-time user reach a useful input without understanding Seats first. Existing multi-Seat protocol stays available for deeper work.
- **Expected information gain:** local flow and browser checks will show whether first-screen confusion and objective leakage are caused by the current entry and shared objective state. No provider call is needed to validate layout, routing, state isolation, or disabled-call boundaries. Actual Solo usefulness and Quick Council repeat use belong to later evidence gates.
- **Acceptance checks:** fresh entry exposes the four named paths with honest availability; Chat reaches a Solo input with one Connection; a second Seat is required only for a Council or existing multi-Seat Pack; switching modes restores that mode's own draft and never carries another mode's objective; existing saved Review/Plan rooms reopen with their exact original objective; build, 63 existing tests plus targeted state/flow cases, lint, type check, desktop/mobile first-screen inspection, and `git diff --check` pass. An unavailable path must not look like a working action.
- **Cost and safety boundary:** zero paid model calls during development or validation, zero deployment, no new provider, no credential persistence, no broad visual redesign, no generic Pack runtime, no automatic Council run. Preserve session BYOK and room history. Create a named recoverable backup of the pre-UI commit before frontend changes.
- **Stop condition:** if the first slice cannot provide Solo without modifying the established Review/Plan persistence or provider security boundary, stop after the baseline and revise the slice. Do not claim DP-0.3 complete from source tests alone; a first-screen browser pass and mode-switch check are required.

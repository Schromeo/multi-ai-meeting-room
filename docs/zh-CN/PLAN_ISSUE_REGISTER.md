# Plan 问题报告清单

更新：2026-08-29。当前M2.13纠错的统一清单，逐项更新，不把本地通过当真实产品通过。

## 核心验收

交付完整、具体、能被独立批评改善、可由用户采用的计划。模型多、思考久、JSON合法、测试通过都不能替代它。[010](evaluations/2026-08-27-plan-quality-live-010.md)和[011](evaluations/2026-08-27-plan-wait-continuation-011.md)仍是负面证据：只完成六天，实际计划审阅未运行。假设不能当成011已证实根因。

状态：待处理、处理中、本地修复（真实未验证）、已验证、暂缓。每项单独附证据关闭。

| 编号 | 优先级 / 状态 | 证据与问题 | 解决方案与验收 |
| --- | --- | --- | --- |
| PLAN-01 | P0 / 本地修复（真实未验证） |011没有新增有效天数，不完整响应和解析拒收都报同一种错。 | 新Builder/Reviewer尝试在检查点和历史保存有限结束/用量与具体拒收分类；离线路由区分截断、JSON、结构、算术、跨日冲突及传输失败，保住有效天数、不重试。旧记录缺失证据仍未知。 |
| PLAN-02 | P0 / 本地修复（仅新格式指令） |010临时交叉审阅JSON修复仍进入Builder；存在已证实，致因未证实。 | 新增格式专用指令限定阶段/轮次，后续Builder不接收。实际prompt与历史测试通过，长期需求和旧correction保留；不静默迁移010/011及其冻结Plan状态。 |
| PLAN-03 | P1 / 处理中（本地配置，需真实证据） | 推理与可见交付共用恢复9,600输出上限；011取整减少约9K，不证明耗尽。 | D-058只把已识别GPT-5 Builder调为low，语义判断继续medium，其他供应商保持默认，并在实际用量旁记录请求档位。离线payload/历史通过且不加上限；最终仍须获准单阶段验证交付完整天数及结束/用量。 |
| PLAN-04 | P1 / 本地修复，真实超180秒未验证 |010在180秒被截断。 | D-056取消Plan应用截止/累计时间停止；模拟600秒与取消通过。011不到180秒，不冒充长请求完成。 |
| PLAN-05 | P1 / 本地修复 |010前置格式恢复挤占产物恢复名额。 | D-056显式兑现一次产物恢复，保留预留/token；011确实启动一次、不自动重试。请求账本另算。 |
| PLAN-06 | P1 / 新Plan已验证 | D-062在Builder/Reviewer供应商工作前发出并保存严格的阶段`started`回执，usage明确未知，再由同请求/阶段的接受、拒绝或供应商错误终态原位替换。Reviewer只有Builder接受后才有回执。 | 构建、62项测试和lint通过，覆盖历史往返、顺序、替换与失败；凭证/原始输出/私有推理继续排除。历史未知调用不重建，未知不等于零或退款，供应商账单仍权威。 |
| PLAN-07 | P1 / 待处理 | 元数据已有错，如#53标Easy；未接题库。 | 官方来源绑定核验或明确元数据未验证Gate，不能编造查证或放宽校验。 |
| PLAN-08 | P1 / 待处理 | Day1用2Easy替1Medium却说等量；替代题不明确、时间和顺延可能破约束。 | 替代任务明确化，确定性重算工作量/时间及下游影响；保留用户最低量，未完成就如实标未完成。 |
| PLAN-09 | P1 / 待处理 | 基础/时间假设、新做与重做比例、时间盒可能只追数量。 | 分清用户硬要求、可用时间假设、审阅建议；按掌握、间隔、先修关系和可执行调整评价。重做并非自动无效，复习时间要包括限时重做。 |
| PLAN-10 | P1 / 待处理 | 真实实验014已完成实际产物并由Fable读取，但非法JSON使整份审阅拒绝；仍无有效意见、修改、复核或采用，D-055修改/复核仍只有脚本证据。 | D-060已离线提高实际产物审阅契约可靠性。下一步针对保留的Sol产物取得具体有据意见、修改受影响内容、复核并交给用户采用；不重生成、不强迫一致。 |
| PLAN-11 | P2 / 已验证 | D-061区分持久化预算停止与人工停止，旧房间保持中性；结构合法的Plan恢复在任一控件启用前按保留用量评估。 | 构建、62项测试和lint通过。额度耗尽恢复可见但禁用，handler再次检查预算，不能启动供应商调用。不迁移旧记录；D-062处理新Plan阶段回执，PLAN-12持久连续性仍独立待处理。 |
| PLAN-12 | P2 / 待处理 | 重绑模型重置活动视图，Overview藏Plan进度，切换/刷新可能中断付费。 | 可预期恢复原席位、显示产物进度、保留持久检查点；未来持久运行器不混入当下小修。 |
| PLAN-13 | P1 / 本地修复（真实未验证） | 旧交叉审阅曾JSON失败并切换语言。真实实验014中Fable完成5,191输出token的实际产物审阅，但2,720可见字符不是合法JSON；原文按设计未保存。 | D-060只向明确受支持的Anthropic Plan Reviewer发送原生JSON Schema，本地语义限制继续权威；只接受单独JSON围栏，拒绝说明文字包裹且不重试。构建、62项测试和lint通过；仍需一次保存产物Reviewer-only真实结果。语言一致性仍主要由prompt约束，尚无独立确定性validator。 |
| PLAN-14 | P2 / 待处理 | 角色仍继承通用会议结构，加席可能只增开销/共同错误。 | 检验课程设计、可行性、实际产物审阅的独特贡献，只为明确缺口加席。 |
| PLAN-15 | P2 / 待处理 | 没有优于单强模型的配对证据。 | 有可用产物后比实质改进、误批、成本和人工负担；M2.12继续暂缓而非通过。 |
| PLAN-16 | P3 / 暂缓 | 三项Cloudflare类型声明错误，部署版与本地不同。 | 单独环境维护，不是010/011已证实根因，不混入本轮纠错。 |
| PLAN-17 | P0 / 已验证 | 真实实验012历史12直达Plan synthesis，但API仍要求两份proposal和一份review，在供应商前本地拒绝。 | 通用synthesis历史前置条件现排除Detailed Plan；真实实验014以空讨论历史进入产物工作并恰好发出预期两调用，普通Decide仍保留Gate。 |
| PLAN-18 | P0 / 已验证 | 真实实验012历史13发出绑定新Canonical State version0的合法空Plan，但客户端解析器要求version>=1，保存前中止。 | Plan产物解析器允许来源version0、拒绝负数；真实实验014从新version-zero房间在审阅前持久化全部12天。 |

## 执行顺序与停止规则

1.先01诊断闭环，再02指令作用域，不新增付费调用。
2.03依据真实证据；06/11只在阻挡显式恢复时优先。
3.07-10交付正确性和批评到修改，不能以schema通过代替采用。
4.完整产物链证明有效后再处理12-15，16单列。

每个切片走[开发纠错环](DEVELOPMENT_CORRECTION_LOOP.md)，记录改动/测试/未解决项，同步此表中英文。不自动重跑付费、不重写平台、不加通用agent、不无限优化。

## 2026-08-29：产物优先入口收尾

- D-059和[真实实验012](evaluations/2026-08-29-plan-artifact-first-live-012.md)把六调用通用启动替换为一个Builder加一个实际产物Reviewer；预检与首次协议预算为两次调用/22K输出。
- PLAN-17/18已本地修复并有行为覆盖；构建、61项测试和lint通过。产物及Reviewer质量Gate仍未运行。
- 历史12没有供应商调用；历史13在客户端取消前可能已启动Sol，没有有效用量，D-062不会回造。新Plan请求现在保留阶段生命周期回执，但新的两调用运行仍不能在未刷新授权时沿用原上限。
- 刷新授权后的[真实实验014](evaluations/2026-08-29-plan-artifact-first-live-014.md)验证PLAN-17/18：Sol持久化12/12天，Fable在恰好两调用中收到实际产物。Fable非法JSON使PLAN-10/13继续待处理。停止付费，保留完整草稿；下一项只纠正Reviewer且先离线。

## 2026-08-29：PLAN-13 本地契约收尾

- 见[纠错简报](correction-briefs/2026-08-29-plan-reviewer-structured-output.md)、D-060。受支持的 Anthropic Plan Reviewer 现在接收带窄审阅 JSON Schema 的 `output_config.format`，覆盖 Fable 5。不受支持 Claude ID 及无关 OpenAI/Gemini 调用保持旧 payload 和供应商默认 thinking。
- 本地 Plan 校验继续限制请求对应日期、字段长度与集合数量。整份响应只有一个 JSON 围栏时可归一化；JSON 外存在说明文字仍非法。不保存原始响应、不增加隐藏提取调用、自动重试、模型/上限/角色 prompt 改动或 Plan 重生成。
- 构建及 62 项离线测试通过，lint 通过；类型检查仍仅三项既有 Cloudflare ambient type 错误。零供应商调用、零 API 花费。备份：`/private/tmp/meeting-room-before-plan-reviewer-structured-20260829.tar.gz`。
- **停止：** PLAN-13 已本地修复但真实未验证，PLAN-10 继续待处理。下一项有信息量的动作最多是一次重新明确授权的 Fable Reviewer-only 调用，读取保留的 Live014 产物，随后检查意见是否接受及语义质量；该验证绝不重跑 Sol。

## 2026-08-27：01和02本地收尾

- 改动`lib/plan-artifact.ts`、`lib/meeting-state.ts`、`app/api/discuss/route.ts`、`app/page.tsx`、`app/plan-view.tsx`和`tests/rendered-html.test.mjs`。见[纠错简报](correction-briefs/2026-08-27-plan-diagnostics-scope.md)、D-057。
- 保存最近四次初始Builder/Reviewer尝试摘要，每次最多十二条拒收明细及拒收总数。只记录供应商实际返回的用量和结束状态，缺失为null而非零。不存原始响应、密钥、私有推理，诊断不进入Plan模型上下文。
- 验证：构建、59项离线测试、lint通过。覆盖三家适配器、仅推理耗尽、传输中断、缺少结束事件、审阅JSON错误、记录往返和实际prompt阶段隔离。仍有三项既有Cloudflare类型错误；本轮未新增浏览器交互或IndexedDB故障测试。本地HTTP200。
- 零真实API调用，不部署、不换模型、不改输出额度、不自动重试、不改归档天数。修改/复核还没有此类尝试摘要；已报告token不是权威账单，本轮也不采集供应商request ID。
- 下一项：PLAN-03有据调整输出/推理。无法追溯011已经丢失的原始响应；后续真实验证需要明确剩余预算授权，并明确如何处理旧全局correction，不能悄悄重试。完整Plan质量、独立审阅和人工采用仍未验证。

## 2026-08-29：PLAN-03本地配置

- 见[纠错简报](correction-briefs/2026-08-29-plan-reasoning-headroom.md)、D-058。已识别原始GPT-5 ID的Builder请求`low`，实际Plan Reviewer/Editor/Recheck请求`medium`，普通讨论仍`minimal`。Anthropic、Gemini及未识别模型保持供应商默认，不猜测跨模型thinking参数。
- 尝试诊断分开显示请求档位和实际报告reasoning token。缺此字段的旧诊断继续有效，未知枚举拒收；折叠Plan视图显示两者，仍不保存推理内容。
- 输出上限、缺失天动态额度、prompt、校验、模型、调用预算和重试行为不变。9,600仍是缺六天时的请求上限，不保证为可见正文预留。
- 构建及59项离线测试通过，覆盖三家实际mock payload/default、历史兼容、界面披露、截断和部分天数保留。Lint/type/本地HTTP见开发日志。零真实调用。
- **停止：** 当前不算完成或真实通过，不能沿用010旧授权消费。下一项有信息增益的动作，是同模型/契约/上限的一次显式授权Builder阶段验证，再检查结束状态、请求档位、报告reasoning/output及有效天数。low仍耗尽或伤害产物时保留失败并重评，不自动重试。

# M2.12 纠错简报：回执访问与估价来源

日期：2026-08-27
状态：本地完成

## 问题与产物

S1 009 已生成可用简历，但内置浏览器未能取回下载，只保存了可见输出及重建的提示元数据。应用使用供应商通用费率，页面 $0.0038 与模型单价计算的 $0.00547 不同。

## 最小假设与验收

- 直接展示现有服务器回执的可选中只读文本，增加复制及手动回退。保留下载和页面内存语义，不新增存储、接口、重跑或伪造历史回执。
- 新 Replay 响应（含 S1 失败回执）记录实际输入/输出费率、各自默认值/覆盖来源。明确是供应商通用估价，不是已验证模型价或账单。不改现有算术，未知用量仍未知。
- 用本地模拟验证完整回执、复制成功/被拒、旧回执、费率来源及覆盖/失败情况，不新增价格目录或模型选择逻辑。
- 不改固定任务、提示、上限、会议阶段、凭证或 oracle 边界。不大修前端，保留现有源码作为恢复依据。

## 费用、信息增益与停止

零真实供应商调用，不重跑 009，不使用旧预算。回执无需下载可复制、估价依据明确后即停。本轮改善可审计性，不证明模型质量；M3/R6 和多模型独有价值仍未验证。同步中英文文档，不自动进入付费对照。

## 结果

已增加默认折叠的完整只读回执、复制及选中文本回退。保留下载、失败与未知用量。新 Replay 回执记录实际供应商费率及各项默认/覆盖来源，旧回执明确未记录来源。不改价格、提示、调用数量或持久化。构建和 40 项测试通过；复制失败路径为本地测试，未在真实浏览器测试。现有 localhost:3001 响应正常，真实供应商调用为零。009 证据文件不变，不把缺失历史元数据重建成事实。下一步回到产品比较决策，不再扩建基础设施。

# M2.12 纠错简报：S1 简历基线 009

日期：2026-08-27
状态：一次调用完成并评分；服务器回执元数据不完整

## 问题、产物与假设

Review 流程已有诊断历史，却没有相同输入的单模型产物，尚不能证明新增编排带来改进。本次检查一个非 mini 强模型能否仅凭固定公开证据，交付完整合成简历、实质修改说明和待人工核实项。

## 固定配置与验收

- 案例 `resume-truth-v1`，共用 S1 提示保持不变。不加入评分答案、额外材料、外部检索或 Chair 干预，不提取隐藏状态或 key。
- 应用为本地未提交 v0.11j 加固定 S1 入口；HEAD `d844b403a323c675f64e0f6e543a24beb606db07` 不代表完整工作区版本。
- 从已发现列表选择 OpenAI `gpt-4.1-2025-04-14`。这是非推理基线，不宣称最新或最强。temperature/reasoning/thinking 不传，输出上限 2,400 token，使用现有超时；一次调用、无重试。
- 保存完整无凭证回执，验证输入/提示哈希后评分。先看实际产物，再按既定规则评估发现、修复、保留、误报和可用性。收到响应不等于质量通过。
- 评分者为 Codex，非盲评，且与 fixture 编写者不独立。不把评测结论当作真实用户采纳或阅读时间结果。

## 预算与停止条件

用户本轮批准 `$0.10`。2026-08-27 核对的 [GPT-4.1 官方页面](https://developers.openai.com/api/docs/models/gpt-4.1) 标准单价为输入 $2/百万 token、输出 $8/百万 token。system/user 文本为 2,719 UTF-8 字节；按含封装余量的 2,919 输入 token 和完整 2,400 输出上限保守预估 $0.025038。这是标准价格规划估算，不是强制美元限额或账单。

调用前 Meetings 为 10，Connections 为 2。首次结果或失败后即停。失败如实保留部分输出并标记未知用量。不重试，不跑 M3/R6/Plan，不在失败后换模型，不自动消耗剩余预算，不部署或修改运行时代码。先保存证据，再判断后续比较还能回答什么问题。

## 结果

见[基线 009](../evaluations/2026-08-27-m2.12-s1-resume-baseline-009.md)。一次调用显示 543 输入 / 548 输出 token、6.0 秒，修复三个预设问题；简历可用性 2/2，但待核实项偏泛、说明重复。Meetings 仍为 10。页面估算 $0.0038，按官方标准价重算为 $0.00547，不是账单。付费测试已停止。

内置浏览器未能取回下载回执，改为保存完整可见输出与重建的预期输入/提示哈希；服务器回执哈希比对、请求 ID、准确时间戳仍不可用。未提取 key 或隐藏状态。下一步只做零调用证据/估价来源修正，不再次运行 S1/M3/R6/Plan。

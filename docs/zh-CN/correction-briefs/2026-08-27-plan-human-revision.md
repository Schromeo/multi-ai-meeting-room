# 人工修订计划

状态：本地实现完成，浏览器/供应商验收仍开放

结果：构建、48 项离线测试和 lint 通过，覆盖原版来源、多天修改、准确批准/历史与保存处理函数注入失败。这不是真实 IndexedDB 故障或用户采用测试。零 API 调用；教学质量、阅读负担与比较优势未知。实现到此停止，保留真实浏览器和供应商质量 Gate。

已观察缺口：详细 Plan 显示逐天审阅意见，却只能整份批准/拒绝。有效建议不能在房间内变成可采用的修改。基线：首个 M2.13 切片保存 AI 每日计划和审阅，但内容改变需要新房间。

最小假设：批准前，Chair 可以本地修改一天；完整修订计划仍通过不变的天数/MEU/时间契约检查，可以查看原版，并冻结准确修订产物。原计划和模型审阅保持不变，不暗示模型审阅了人工改动。

边界：不调用付费模型、不自动改写、不增加轮数、不修改契约、不引入外部题库或通用编辑框架。复用本地历史与批准，编辑题目、时间、完成标准及调整文字，其他天保持不变。保存成功后才发布修订，未保存编辑阻止批准；已批准/拒绝计划只读。不新增教学质量或比较优势结论。

验收：格式错误、重复题、工作量不足、超时及过期来源拒绝；原版不变；连续修改保留其他天；恢复与批准准确匹配可见修订；保存失败保留草稿和此前已保存版。仅离线 fixture/渲染和接口回归，真实浏览器与供应商质量 Gate 仍开放。补齐采用路径后停止，不开启新编排里程碑。

备份：`/private/tmp/meeting-room-before-plan-human-edit-20260827.tar.gz`。

# M2.12 S1 简历基线 009

日期：2026-08-27
状态：一次调用完成，已评估产物；回执元数据不完整。M3/R6 未运行。

## 结果与边界

OpenAI `gpt-4.1-2025-04-14` 一次调用交付了完整、可采用的合成简历，独立发现并修复三个预设问题。但说明并不完美：重复解释一次删除，待人工确认项偏泛，没有给出最有用的后续核实动作。这是已知校准案例，不代表一般质量，也不是已完成多模型比较。

见[调用前纠错简报](../correction-briefs/2026-08-27-m2.12-s1-baseline-009.md)、[评分规则](M2.12_REVIEW_COMPARISON.md)、[未改写的完整可见输出](../../evaluations/artifacts/s1-resume-009/raw-output.txt)、[结果面板](../../evaluations/artifacts/s1-resume-009/visible-result.txt)和[采集清单](../../evaluations/artifacts/s1-resume-009/capture.json)。

## 执行与证据限制

- 用户批准 `$0.10`；只启动一次 S1，无重试、无其他供应商调用。Meetings 前后均为 10。无运行中干预、无生成后人工修改，不改运行时代码。
- 使用固定版本、非 mini、非推理 GPT-4.1，不宣称最新。源码配置为 2,400 输出 token；不传 temperature/reasoning/thinking。模型只收到固定公开任务，不含 oracle 或判分反馈。
- 本地脏工作区包含 v0.11j 和 S1 入口，仅靠 HEAD `d844b403a323c675f64e0f6e543a24beb606db07` 无法复现完整版本。
- 页面 `pre` 中完整可见输出原样保存，共 2,695 字符。各部分完整，输出未达上限。适配器不保留供应商结束状态，不能把人工完整性检查当成已知 transport finish status。
- 内置浏览器未能取回下载回执：等待下载事件超时，content export 明确不支持。下载点击不调用模型，在检查的下载位置未找到 JSON。不能据此断言应用下载在所有浏览器均失效，也不能声称保存了逐字一致的服务器回执。
- [预期输入](../../evaluations/artifacts/s1-resume-009/expected-input.json)和[预期 system/user 提示](../../evaluations/artifacts/s1-resume-009/expected-prompt.json)来自未修改的共用公开构造器。清单保存其哈希，但无法与缺失的服务器回执比对。请求 ID、准确时间戳、准确毫秒和账单均未知。后续复用必须保留此限制，不为补元数据重跑模型。

## 先看产物，再判分

评分者为 Codex，非盲评，与 fixture 编写者不独立。先检查原始输出 1-16 行的简历，再看解释。Oracle 不是独立留出集或穷尽答案。

| 问题 | 权重 | 发现证据：原始输出行号 | 实际简历的修复证据 | 判定 |
| --- | ---: | --- | --- | --- |
| `resume-metric` | 3 | 23-25 行指出 50% 无测量依据 | 第 9 行只说分诊更容易，没有数字，符合候选人记录 | 发现并修复 |
| `resume-placeholder` | 3 | 26-28 行说明缺少 AI 工作证据 | 6-9 行无 AI 模板或虚构替代经历 | 发现并修复 |
| `resume-absolute` | 2 | 34-36 行指出不能覆盖所有任务 | 第 13 行限定为部分 indexing jobs 的结果复用 | 发现并修复 |

- 评估覆盖 3/3；加权发现 8/8（100%），完整修复 8/8（100%），没有部分或未知项。百分比只描述这个小案例。
- 4/4 项受保护含义保留：教育、API 工作、SignalSearch 有限减少、技能；任职日期也保留。SignalSearch 增加了有依据的范围限定，不计为新发现。
- 无依据的实质 Finding 为 0，有害采纳修改为 0。没有发明毕业资格门槛，也没有把 `reduce` 说成完全消除。提醒毕业后时间可能需检查只是低价值提示，不是已证实风险或可计分发现。
- 简历可用性 **2/2**：在这个合成任务中不需再做实质真实性修补。仅为评测者判断，不等于用户已采纳，也不代表真实招聘效果。
- 待人工核实项 **偏弱**：47-50 行主要讲外部验证，未明确要求恢复数字前先取得测量证据、补 AI 经历前先确认真实案例。简历里已无相应虚假声明，因此不否定简历可用性，但伴随交付的可操作性不足。
- 阅读负担：26-28 行和 38-41 行重复说明占位符删除，不重复计分。真实用户阅读/编辑时间未知，不记为零；没有用户采纳版本，原始输出就是未经干预的候选版。

## 费用与体验

| 指标 | 观察或计算值 |
| --- | --- |
| 显示输入 / 输出 token | 543 / 548 |
| 显示时长 | 6.0 秒，已四舍五入 |
| 页面估算 | $0.0038 |
| 官方标准价重算 | $0.00547 |
| 账单 / 单独 reasoning 用量 | 未知 / 未报告 |
| 调用 / 重试 | 1 / 0 |

2026-08-27 核对的 [GPT-4.1 官方单价](https://developers.openai.com/api/docs/models/gpt-4.1)为输入 $2/百万 token、输出 $8/百万 token。不假设缓存折扣，计算 `(543 × 2 + 548 × 8) / 1,000,000 = $0.00547`，低于 $0.10 授权；不是供应商账单。

差异有源码依据：`app/api/discuss/route.ts` 的 OpenAI 默认估价是供应商级输入 $1/百万、输出 $6/百万，不是此模型单价。按该默认值计算 $0.003831，与页面四舍五入后吻合。没有检查运行时环境覆盖，因此不宣称已确认实际配置。本次不修改价格表。

## 纠偏决定与下一步

**停止付费测试，保留 S1。** 单模型已经修复所有预置事实缺陷，后续模型不能对同样修复再领取“独有增益”。更具体的人工核实动作、减少审阅噪声仍可能改善，但必须实际观察并与额外成本比较，不能预设多模型会更好。

下一小步是零供应商调用的证据可取回性和估价来源说明：让现有回执不依赖下载也能保存，区分通用估价与模型专属估价。不扩建模型市场、通用评测平台、agent 或大改 UI。不为补元数据重跑此次 S1。后续 M3/R6 必须有明确待解问题和新授权，不自动消耗剩余预算。

M2.12 仍为当前阶段，M2.11 扩建冻结。Benchmark 005 的输入、模型和上下文不同，不能据此计算优势。配对比较尚未运行，多模型独有增益和交叉审阅因果价值仍未知。

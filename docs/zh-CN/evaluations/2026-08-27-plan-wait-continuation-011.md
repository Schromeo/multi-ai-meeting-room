# Plan等待续接011：仍未完整交付

## 范围与结果

续接[010](2026-08-27-plan-quality-live-010.md)，不新开会议、不新增$5授权。同一GPT-5快照/Opus4.7、议题及12天/10MEU/360分钟测试契约。D-056取消Plan时间截止，并显式预留两次恢复调用；不改prompt/模型，不加席、不修改、不自动重试。

- 北京时间22:44:54点Resume，22:46:50观察到失败；本次不是超过180秒的真实长请求证明。
- 新增一次Builder请求。供应商适配器返回并计入用量，但解析器没接受任何新天数；错误为Day7-12缺失或工作量/时间/重复校验不通过。实际计划审阅未启动。
- 仍6/12天。原77条任务、每日标题、复盘/完成说明、调整规则均与010归档一致。历史仍11条，未删除或批准原记录。
- 本次不再被180秒或累计时间拦截，但完整交付、语义质量、审阅、修改和采用仍失败或未测，不算产品通过。

## 证据与边界

- [失败界面](../../evaluations/artifacts/plan-wait-011/01-continuation-failure.txt)、[界面中的六天原稿](../../evaluations/artifacts/plan-wait-011/02-retained-days.json)、[预检](../correction-briefs/2026-08-27-plan-wait-recovery.md)。原始证据共享英文目录。
- 剩余输出显示46K->37K（四舍五入），调用名额剩0。名额是保守transition预留，不等于实际计费调用；此次只用一次、不是两次。010/011累计已知真实请求七次。
- Builder输出上限9,600，潜在第二次审阅上限6,000。当前Plan界面/留档不提供精确新增输入/输出/推理分项、结束原因、被拒原文、请求ID或账单。接近额度提示输出预算压力，但不能证明推理耗尽或JSON错误；不能编造精确费用，也不能因无交付就说没扣费。仍受原$5总授权约束，没有再发付费请求。
- 源码确认：Builder把不完整响应及拒收日记录统一成缺失天数；解析器静默丢弃JSON/schema/整体验证失败。上一阶段“只输出短Turn Envelope、不写每日题单”的临时Chair指令仍进入Builder的canonical context。其存在已证实，是否造成这次失败尚未证实。

## 下一步顺序

| 优先级 | 问题 | 最小修复与验收 |
| --- | --- | --- |
| P0 | 失败不可诊断 | 保存有限且不含凭据的结束状态、供应商提供的可见/推理token分项、逐天拒收原因；fixture区分截断、JSON错误、算术错误、跨日冲突，不为补遥测再烧一次真实调用。 |
| P0 | 临时阶段指令污染后续 | 把交叉审阅格式修复限于对应阶段，保留真实用户需求，不传递过期Envelope/限字指令；断言Builder实际prompt不再包含它。 |
| P1 | 推理与交付共用缩小的输出额度 | 有诊断后再按可见交付预留空间，每次只验证一个有依据的配置变化；不盲目最高推理、全面加额度，或为已取消的计时器拆任务。 |
| P1 | 恢复与计费混为一谈 | 保留失败回执，区分已启动请求与预留席位，仍只显式恢复、不自动重试。预算耗尽时不再展示会被预检拒绝的可点恢复入口。 |
| P1 | 草稿质量仍不可靠 | 原题目元数据、替换MEU/时间算术、未指定替代题、顺延影响仍有缺口。先审阅真实完整计划，再修指定意见，不偷偷降题量；跨日改动需明确范围，不能静默改已保存天数。 |
| P2 | 等待/恢复含义误导 | 持久化准确停止原因，修复预算停止重开后显示Human Chair stop；简化原席位匹配，Overview也显示Plan进度，不扩展大改版。 |
| P2 | 核心价值未证实 | 完成交付后才检验独立批评是否改善成果；JSON通过、思考更久、席位更多都不等于有效。D-055真实修改/复核与配对对照仍未测。 |

## 本地验证

最终stopped/complete判断修复后，构建和54项离线测试通过，lint结果见开发日志。真实适配器包装的模拟计时推进600秒，验证取消及普通90秒策略；不冒充真实超过180秒测试。保留三项既有Cloudflare类型声明错误。浏览器发现fixture把实际停止误写成paused后，已改用生产stopProtocol构造。无部署、无key读取。

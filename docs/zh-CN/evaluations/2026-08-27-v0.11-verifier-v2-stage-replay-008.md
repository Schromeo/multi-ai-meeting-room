# v0.11 Verifier v2 Stage Replay 008

**日期：** 2026-08-27

**状态：** 通过。Anthropic Haiku 在一次隔离的真实供应商调用中完成 Review Verifier v2 契约。

**运行边界：** 一次调用、600 输出 token 上限、无重试、不修改 Meeting，用户授权的应用估算上限为 `$0.01`。供应商账单仍是权威依据。

## 配置

- 服务端持有的匿名 Review Verifier fixture version 2。
- 通过 session-only BYOK 连接调用 Anthropic `claude-haiku-4-5-20251001`。
- 使用生产 changed-material prompt、Anthropic adapter 与严格 v2 parser。
- 一项 declared Change、一项已接受 Finding，以及提供的来源文本。
- 要求同一个 Change ID 分别给出独立的 `lineage` 与 `semantics` verdict。

## 结果

- 契约结果：`pass`。
- 模型只为 `change-replay-1` 返回一项 check，且没有 unresolved 项。
- `lineage` 为 `supported`：说明把 replacement 绑定到已接受 Finding 与提供的来源。
- `semantics` 为 `supported`：说明独立检查了有限改写没有引入不受支持的指标、日期、所有权、影响或资格声明。
- Summary 描述了实际变更材料，而不是只表示同意 Editor。
- Replay 前后 Meeting History 都显示 10 条记录，没有写入 Meeting。
- 没有重试、切换模型，也没有继续运行完整 Review 或 Plan 房间。

## 用量

| 指标 | 结果 |
| --- | ---: |
| 供应商调用 | 1 |
| 输入 tokens | 597 |
| 输出 tokens | 224 |
| 模型耗时 | 3.1 秒 |
| 应用估算 | `$0.0034` |

## Gate 结果

**Verifier v2 真实供应商 Gate 通过。**

结果证明所选 Anthropic 模型能在隔离 stage 边界内满足双重 lineage/semantics Artifact 契约。它关闭的是供应商格式不确定性，而不是 Review 产品质量：Finding recall、cross-review 价值、Editor 质量、用户阅读负担，以及相对强单模型 baseline 的质量/成本优势，仍需在 M2.12 对照中验证。

## 下一动作

不增加更多 Review 机器，也不再运行付费房间。进入 M2.12 证据路径：先定义共享 rubric，并保存强单模型与人工 copy/review baseline，再选择能判断结构化多模型 Review 是否值得其成本与复杂度的最小对照。
